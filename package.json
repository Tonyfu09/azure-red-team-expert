{
  "name": "swlabexploit",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "sh -c 'mkdir -p build && cp -r ./src/* ./build'"
  }
}
